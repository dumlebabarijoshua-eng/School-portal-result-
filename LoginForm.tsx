 "use client"; import {useState} from "react";import{useRouter}from"next/navigation";export default function LoginForm(){const[u,setU]=useState(""),[p,setP]=useState(""),[e,setE]=useState(""),[loading,setL]=useState(false),router=useRouter();async function go(x:React.FormEvent){x.preventDefault();setL(true);const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:u,password:p})});const d=await r.json();setL(false);if(!r.ok)return setE(d.error);router.push(d.role==="ADMIN"?"/admin":d.role==="TEACHER"?"/teacher":"/student")}return <form onSubmit={go}><label>Username / Matric Number / Staff ID</label><input className="input" value={u} onChange={x=>setU(x.target.value)} required/><label>Password / PIN</label><input className="input" type="password" value={p} onChange={x=>setP(x.target.value)} required/>{e&&<p style={{color:"crimson"}}>{e}</p>}<button className="btn" disabled={loading}>{loading?"Signing in...":"Sign in"}</button></form>}