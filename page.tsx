import{requireRole}from"@/lib/auth";import{db}from"@/lib/prisma";import Link from"next/link";export default async function Results(){const u=await requireRole("STUDENT"),s=u.student!;const rs=await db.result.findMany({where:{studentId:s.id,status:"PUBLISHED"},include:{subject:true,academicSession:true,term:true},orderBy:[{academicSession:{name:"desc"}},{createdAt:"desc"}]});return <main className="main"><Link href="/student">← Dashboard</Link><h1>Result History</h1><div className="table-wrap"><table className="table"><thead><tr><th>Session</th><th>Term</th><th>Subject</th><th>CA</th><th>Exam</th><th>Total</th><th>Grade</th><th>Remark</th></tr></thead><tbody>{rs.map(r=><tr key={r.id}><td>{r.academicSession.name}</td><td>{r.term.name}</td><td>{r.subject.code} — {r.subject.name}</td><td>{r.caScore}</td><td>{r.examScore}</td><td>{r.totalScore}</td><td>{r.grade}</td><td>{r.remark}</td></tr>)}</tbody></table></div></main>}